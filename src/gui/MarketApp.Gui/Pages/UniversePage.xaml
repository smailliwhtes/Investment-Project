<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarketApp.Gui.Pages.UniversePage"
             Title="Universe">
  <ContentPage.Resources>
    <ResourceDictionary>
      <Style x:Key="InfoIconButtonStyle" TargetType="Button">
        <Setter Property="Text" Value="i" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="WidthRequest" Value="22" />
        <Setter Property="MinimumWidthRequest" Value="22" />
        <Setter Property="HeightRequest" Value="22" />
        <Setter Property="MinimumHeightRequest" Value="22" />
        <Setter Property="CornerRadius" Value="11" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="BackgroundColor" Value="#2F2F2F" />
        <Setter Property="BorderColor" Value="#4E6FA4" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="TextColor" Value="#FFFFFF" />
      </Style>

      <Style x:Key="PageTitleStyle" TargetType="Label">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontAttributes" Value="Bold" />
      </Style>

      <Style x:Key="SectionTitleStyle" TargetType="Label">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontAttributes" Value="Bold" />
      </Style>

      <Style x:Key="BodyLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="14" />
      </Style>

      <Style x:Key="TableHeaderStyle" TargetType="Label">
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="FontSize" Value="13" />
      </Style>

      <Style x:Key="TableCellStyle" TargetType="Label">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="LineBreakMode" Value="TailTruncation" />
        <Setter Property="MaxLines" Value="1" />
      </Style>

      <Style x:Key="InputEntryStyle" TargetType="Entry">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinimumHeightRequest" Value="40" />
      </Style>

      <Style x:Key="PickerStyle" TargetType="Picker">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinimumHeightRequest" Value="40" />
      </Style>
    </ResourceDictionary>
  </ContentPage.Resources>

  <ScrollView>
    <VerticalStackLayout Padding="18" Spacing="14">
      <HorizontalStackLayout Spacing="6">
        <Label Text="Scored symbols" Style="{StaticResource PageTitleStyle}" />
        <Button Style="{StaticResource InfoIconButtonStyle}" CommandParameter="universe_symbols" Clicked="OnInfoClicked" />
      </HorizontalStackLayout>

      <Label Style="{StaticResource BodyLabelStyle}" Text="Search by symbol or theme. Pick one row to see the short takeaway." />
      <Entry Style="{StaticResource InputEntryStyle}" Placeholder="Search by symbol or theme" Text="{Binding FilterText}" />

      <Frame Padding="14">
        <VerticalStackLayout Spacing="8">
          <Grid ColumnDefinitions="1.4*,0.8*,0.8*,2.2*,0.8*" Padding="0,0,0,4" ColumnSpacing="8">
            <Label Style="{StaticResource TableHeaderStyle}" Text="Symbol" />
            <Label Style="{StaticResource TableHeaderStyle}" Text="Fit" Grid.Column="1" />
            <Label Style="{StaticResource TableHeaderStyle}" Text="Age" Grid.Column="2" />
            <Label Style="{StaticResource TableHeaderStyle}" Text="Themes" Grid.Column="3" />
            <Label Style="{StaticResource TableHeaderStyle}" Text="Warnings" Grid.Column="4" />
          </Grid>

          <CollectionView ItemsSource="{Binding FilteredScores}"
                          SelectionMode="Single"
                          SelectedItem="{Binding SelectedScore}"
                          ItemSizingStrategy="MeasureAllItems"
                          HeightRequest="300">
            <CollectionView.ItemsLayout>
              <LinearItemsLayout Orientation="Vertical" ItemSpacing="2" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
              <DataTemplate>
                <Grid ColumnDefinitions="1.4*,0.8*,0.8*,2.2*,0.8*" Padding="0,2" ColumnSpacing="8" MinimumHeightRequest="26">
                  <Label Style="{StaticResource TableCellStyle}" Text="{Binding Symbol}" />
                  <Label Style="{StaticResource TableCellStyle}" Text="{Binding Score, StringFormat='{}{0:F2}'}" Grid.Column="1" />
                  <Label Style="{StaticResource TableCellStyle}" Text="{Binding LagDays}" Grid.Column="2" />
                  <Label Style="{StaticResource TableCellStyle}" Text="{Binding ThemeLabels}" Grid.Column="3" />
                  <Label Style="{StaticResource TableCellStyle}" Text="{Binding FlagsCount}" Grid.Column="4" />
                </Grid>
              </DataTemplate>
            </CollectionView.ItemTemplate>
          </CollectionView>
        </VerticalStackLayout>
      </Frame>

      <Frame Padding="14">
        <VerticalStackLayout Spacing="10">
          <HorizontalStackLayout Spacing="6">
            <Label Text="Symbol detail" Style="{StaticResource SectionTitleStyle}" />
            <Button Style="{StaticResource InfoIconButtonStyle}" CommandParameter="symbol_detail" Clicked="OnInfoClicked" />
          </HorizontalStackLayout>
          <Picker Style="{StaticResource PickerStyle}" ItemsSource="{Binding DetailTabs}" SelectedItem="{Binding SelectedDetailTab}" />

          <VerticalStackLayout IsVisible="{Binding IsOverviewTab}" Spacing="8">
            <Label Text="{Binding SelectedScore.Symbol}" FontAttributes="Bold" FontSize="18" />
            <Label Style="{StaticResource BodyLabelStyle}" Text="{Binding SelectedScore.LastDate, StringFormat='Newest saved price: {0}'}" />
            <Label Style="{StaticResource BodyLabelStyle}" Text="{Binding SelectedScore.GatesPassed, StringFormat='Main checks: {0}'}" />
            <Label Style="{StaticResource BodyLabelStyle}" Text="{Binding SelectedScore.ThemeLabels, StringFormat='Main themes: {0}'}" />

            <ContentView Content="{Binding PriceChart}" HeightRequest="300" />
            <ContentView Content="{Binding IndicatorChart}" HeightRequest="220" />
          </VerticalStackLayout>

          <VerticalStackLayout IsVisible="{Binding IsExplainTab}" Spacing="6">
            <Label Text="Why it ranked" Style="{StaticResource SectionTitleStyle}" />
            <Label Style="{StaticResource BodyLabelStyle}" Text="{Binding ExplainSummary}" />
          </VerticalStackLayout>

          <VerticalStackLayout IsVisible="{Binding IsQualityTab}" Spacing="6">
            <Label Text="Data health" Style="{StaticResource SectionTitleStyle}" />
            <Label Style="{StaticResource BodyLabelStyle}" Text="{Binding QualitySummary}" />
          </VerticalStackLayout>
        </VerticalStackLayout>
      </Frame>
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
