<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarketApp.Gui.Pages.UniversePage"
             Title="Universe">
  <VerticalStackLayout Padding="16" Spacing="12">
    <Label Text="Universe and scored symbols" FontSize="22" FontAttributes="Bold" />

    <Entry Placeholder="Filter by symbol or theme" Text="{Binding FilterText}" />

    <CollectionView ItemsSource="{Binding FilteredScores}"
                    SelectionMode="Single"
                    SelectedItem="{Binding SelectedScore}"
                    HeightRequest="280">
      <CollectionView.Header>
        <Grid ColumnDefinitions="2*,*,*,*,*" Padding="0,0,0,4">
          <Label Text="Symbol" />
          <Label Text="Score" />
          <Label Text="Lag days" />
          <Label Text="Themes" />
          <Label Text="Flags" />
        </Grid>
      </CollectionView.Header>
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Grid ColumnDefinitions="2*,*,*,*,*" Padding="0,2">
            <Label Text="{Binding Symbol}" />
            <Label Text="{Binding Score, StringFormat='{}{0:F2}'}" />
            <Label Text="{Binding LagDays}" />
            <Label Text="{Binding ThemeLabels}" LineBreakMode="TailTruncation" />
            <Label Text="{Binding FlagsCount}" />
          </Grid>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>

    <Frame>
      <VerticalStackLayout Spacing="8">
        <Label Text="Symbol detail" FontAttributes="Bold" />
        <Picker ItemsSource="{Binding DetailTabs}" SelectedItem="{Binding SelectedDetailTab}" />

        <VerticalStackLayout IsVisible="{Binding IsOverviewTab}" Spacing="8">
          <Label Text="{Binding SelectedScore.Symbol}" FontAttributes="Bold" FontSize="18" />
          <Label Text="{Binding SelectedScore.LastDate, StringFormat='Last date: {0}'}" />
          <Label Text="{Binding SelectedScore.GatesPassed, StringFormat='Gates: {0}'}" />
          <Label Text="{Binding SelectedScore.ThemeLabels, StringFormat='Themes: {0}'}" />

          <ContentView Content="{Binding PriceChart}" HeightRequest="280" />
          <ContentView Content="{Binding IndicatorChart}" HeightRequest="200" />
        </VerticalStackLayout>

        <VerticalStackLayout IsVisible="{Binding IsExplainTab}" Spacing="6">
          <Label Text="Explain" FontAttributes="Bold" />
          <Label Text="{Binding ExplainSummary}" />
        </VerticalStackLayout>

        <VerticalStackLayout IsVisible="{Binding IsQualityTab}" Spacing="6">
          <Label Text="Quality" FontAttributes="Bold" />
          <Label Text="{Binding QualitySummary}" />
        </VerticalStackLayout>
      </VerticalStackLayout>
    </Frame>
  </VerticalStackLayout>
</ContentPage>
